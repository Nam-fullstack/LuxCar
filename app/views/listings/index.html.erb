<p id="notice"><%= notice %></p>

<h1>Listings</h1>

<table>
  <thead>
    <tr>
      <th>User</th>
      <th>Variant</th>
      <th>Colour</th>
      <th>Title</th>
      <th>Price</th>
      <th>Mileage</th>
      <th>Description</th>
      <th>State</th>
      <th>Sold</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @listing.each do |listing| %>
      <tr>
        <td><%= listing.user_id %></td>
        <td><%= listing.variant_id %></td>
        <td><%= listing.colour_id %></td>
        <td><%= listing.title %></td>
        <td><%= listing.price %></td>
        <td><%= listing.mileage %></td>
        <td><%= listing.description %></td>
        <td><%= listing.state_id %></td>
        <td><%= listing.sold %></td>
        <td><%= link_to 'Show', listing %></td>
        <td><%= link_to 'Edit', edit_listing_path(listing) %></td>
        <td><%= link_to 'Destroy', listing, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Listing', new_listing_path %>
